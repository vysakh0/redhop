[package]
name = "neorag-benchmarks"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Criterion benchmarks for NeoRAG"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
neorag-core        = { workspace = true }
neorag-chunking    = { workspace = true }
neorag-retrieval   = { workspace = true }
neorag-pipeline    = { workspace = true }
neorag-diagnostics = { workspace = true }
tokio              = { workspace = true }

[dev-dependencies]
criterion = { workspace = true }

[[bench]]
name    = "chunking"
harness = false

[[bench]]
name    = "retrieval"
harness = false
